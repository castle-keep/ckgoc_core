import 'package:flutter/material.dart';

abstract final class SkyGoColors {
  // Primary — Bright Blue (Core)
  static const Color primary = Color(0xFF0070DE);
  static const Color primaryLight = Color(0xFF3394F5);
  static const Color primaryDark = Color(0xFF004DA0);
  static const Color primaryDisabled = Color(0xFFBBD9FA);
  static const Color primaryOnDark = Color(0xFF5EB0F5);

  // Accent — Light Blue Surface (Core)
  static const Color accent = Color(0xFFF4FCFF);
  static const Color accentLight = Color(0xFFFFFFFF);
  static const Color accentDark = Color(0xFFD0EEFF);
}
