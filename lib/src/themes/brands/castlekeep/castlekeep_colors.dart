import 'package:flutter/material.dart';

abstract final class CastleKeepColors {
  // Primary — Deep Forest Green (Core)
  static const Color primary = Color(0xFF02372E);
  static const Color primaryLight = Color(0xFF054D40);
  static const Color primaryDark = Color(0xFF011E19);
  static const Color primaryDisabled = Color(0xFFCBDDDB);
  static const Color primaryOnDark = Color(0xFF68B5AA);

  // Accent — Gold (Core)
  static const Color accent = Color(0xFFE9BB43);
  static const Color accentLight = Color(0xFFF2D07A);
  static const Color accentDark = Color(0xFFC49A1E);
}
